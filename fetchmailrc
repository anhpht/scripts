set daemon 60
set logfile /home/fta/logs/fetchmail.log
poll localhost with protocol IMAP and port 1143
  user "username" there has password "password"
    is fta here and wants mda "/usr/bin/procmail -f %F -d %T"
